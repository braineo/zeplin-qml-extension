{
  "name": "qml-extension",
  "version": "0.1.0",
  "description": "Sample extension description.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish"
  },
  "zeplin": {
    "displayName": "QML",
    "platforms": [
      "web"
    ]
  },
  "dependencies": {
    "zem": "^0.4.4",
    "zeplin-extension-style-kit": "^1.1.0"
  }
}
